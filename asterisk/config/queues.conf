[general]
persistentmembers = yes
monitor-type = MixMonitor
shared_lastcall = no

; File d'attente de démonstration — alimentée par get_queue_stats (AMI QueueSummary).
[support]
strategy = leastrecent
timeout = 15
retry = 5
wrapuptime = 5
maxlen = 0
announce-frequency = 30
servicelevel = 30
member => PJSIP/1001,0,Agent 1001
member => PJSIP/1002,0,Agent 1002
